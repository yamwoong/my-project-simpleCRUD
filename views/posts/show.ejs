<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %> - 게시글 상세보기</title>
    <!-- Bootstrap CDN 추가 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/show.css"> <!-- 외부 CSS 파일 연결 -->
</head>
<body>

<div class="container">
    <h1 class="mb-4"><%= post.title %></h1>
    <p class="text-muted">
        작성 날짜: <%= new Date(post.createAt).toLocaleDateString("ko-KR") %>
    </p>    
    <hr>
    <p><%= post.content %></p>
    <hr>
    <div class="btn-group">
        <a href="/posts" class="btn btn-secondary">목록으로</a>
        <div>
            <a href="/posts/<%= post._id %>/edit" class="btn btn-primary">수정</a>
            <form action="/posts/<%= post._id %>?_method=DELETE" method="POST" style="display: inline;">
                <button type="submit" class="btn btn-danger" onclick="return confirm('정말 삭제하시겠습니까?');">삭제</button>
            </form>
        </div>
    </div>
</div>

</body>
</html>
